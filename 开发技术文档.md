# 智固 App 开发技术文档

## 1. 项目概述

智固 App 是一个面向比赛演示场景的 Android 应用，用于展示“货车紧固辅助与检测”方案。当前版本以本地演示闭环为主，同时保留 DJI 无人机接入能力，满足两类展示方式：

- `demo` 变体：适合模拟器、普通 Android 设备、答辩演示环境，支持本地演示视频替代实时图传。
- `dji` 变体：适合 Android 真机与 DJI 无人机联调，支持 DJI SDK 初始化、设备连接状态展示和实时图传容器接入。

项目目标不是生产落地，而是快速形成可运行、可讲解、可切换演示方案的比赛展示 App。当前版本已经重点强化“辅助感知大屏”模块，用于在比赛演示中模拟类似智能驾驶座舱的可视化感知界面。

## 2. 技术栈与版本

### 2.1 开发语言与框架

- 开发语言：Kotlin
- UI 框架：Jetpack Compose
- 设计体系：Material 3
- 页面组织方式：单 Activity + Compose 多页面切换
- 状态管理：`ViewModel + StateFlow`
- 视频播放：AndroidX Media3 ExoPlayer
- 无人机 SDK：DJI Mobile SDK V5

### 2.2 构建版本

基于 [app/build.gradle.kts](D:/Program/ANDROIDSTUDIO/WORKSPACE/ZhiGu_app/app/build.gradle.kts) 与 [libs.versions.toml](D:/Program/ANDROIDSTUDIO/WORKSPACE/ZhiGu_app/gradle/libs.versions.toml)：

- `compileSdk = 36`
- `targetSdk = 36`
- `minSdk = 24`
- Java 版本：11
- Android Gradle Plugin：`9.2.1`
- Kotlin Compose Plugin：`2.2.10`
- Compose BOM：`2026.02.01`
- Navigation Compose：`2.9.3`
- Lifecycle ViewModel Compose：`2.9.4`
- Media3：`1.8.0`
- DJI Mobile SDK：`5.18.0`

## 3. 工程结构

### 3.1 核心目录

- `app/src/main/java/com/zhiguapp`
  - 应用主入口、主界面、通用 UI、ViewModel、基础运行时
- `app/src/demo/java/com/zhiguapp`
  - 演示版专用图传实现
- `app/src/dji/java/com/zhiguapp`
  - DJI 版专用 Application、SDK 初始化、实时图传实现
- `app/src/main/AndroidManifest.xml`
  - 权限、应用入口、DJI API Key 注入
- `package/`
  - 已打包 APK 输出目录

### 3.2 关键文件说明

- [MainActivity.kt](D:/Program/ANDROIDSTUDIO/WORKSPACE/ZhiGu_app/app/src/main/java/com/zhiguapp/MainActivity.kt)
  - 单 Activity 入口，负责加载 Compose 根界面
- [ZhiGuApplication.kt](D:/Program/ANDROIDSTUDIO/WORKSPACE/ZhiGu_app/app/src/main/java/com/zhiguapp/ZhiGuApplication.kt)
  - 默认 Application，供 demo 变体使用
- [ZhiGuDjiApplication.kt](D:/Program/ANDROIDSTUDIO/WORKSPACE/ZhiGu_app/app/src/dji/java/com/zhiguapp/dji/ZhiGuDjiApplication.kt)
  - DJI 变体专用 Application，启动时初始化 DJI 运行时
- [ZhiGuApp.kt](D:/Program/ANDROIDSTUDIO/WORKSPACE/ZhiGu_app/app/src/main/java/com/zhiguapp/ui/ZhiGuApp.kt)
  - 所有页面 UI 主体、导航结构、权限请求、设置页逻辑
- [ZhiGuViewModel.kt](D:/Program/ANDROIDSTUDIO/WORKSPACE/ZhiGu_app/app/src/main/java/com/zhiguapp/ui/ZhiGuViewModel.kt)
  - 本地业务状态、演示数据、页面交互行为
- [DjiRuntime.kt](D:/Program/ANDROIDSTUDIO/WORKSPACE/ZhiGu_app/app/src/main/java/com/zhiguapp/dji/DjiRuntime.kt)
  - DJI 状态容器，负责对 UI 暴露统一状态
- [DjiSdkController.kt](D:/Program/ANDROIDSTUDIO/WORKSPACE/ZhiGu_app/app/src/dji/java/com/zhiguapp/dji/DjiSdkController.kt)
  - DJI SDK 初始化、注册、监听产品连接与遥测信息
- [DemoVideoPlayer.kt](D:/Program/ANDROIDSTUDIO/WORKSPACE/ZhiGu_app/app/src/main/java/com/zhiguapp/ui/DemoVideoPlayer.kt)
  - 本地演示视频播放组件

## 4. 构建模式设计

### 4.1 Flavor 划分

项目使用 `runtime` 维度，划分两个 product flavor：

- `demo`
  - `applicationIdSuffix = ".demo"`
  - `versionNameSuffix = "-demo"`
  - `BuildConfig.DJI_ENABLED = false`
  - `applicationClass = com.zhiguapp.ZhiGuApplication`
  - DJI Key 为空

- `dji`
  - `BuildConfig.DJI_ENABLED = true`
  - `applicationClass = com.zhiguapp.dji.ZhiGuDjiApplication`
  - 注入 DJI App Key：`5e7c8f3fc9b83dc7a67a9365`
  - NDK ABI 限定为 `arm64-v8a`

### 4.2 运行建议

- 模拟器演示：优先运行 `demoDebug`
- 真机联调 DJI：优先运行 `djiDebug`
- 比赛现场有真机但担心图传不稳定时：
  - 主包使用 `demoDebug` 演示更稳
  - 备用包保留 `djiDebug` 展示真实接入能力

## 5. 应用架构说明

### 5.1 架构模式

当前项目采用轻量级本地展示架构：

- 表现层：Compose 页面
- 状态层：`ZhiGuViewModel`
- 设备运行时层：`DjiRuntime`
- 第三方接入层：`DjiSdkController`

架构特点：

- 无后端依赖
- 无数据库持久化
- 所有演示数据由 ViewModel 本地维护
- 页面状态通过 `StateFlow` 驱动 Compose 自动刷新

在 UI 层中，项目还引入了 Compose 动画与 Canvas 绘制，用于构建比赛演示需要的动态图、扫描波纹、目标框、路径轨迹和状态切换效果。

### 5.2 状态合并机制

`ZhiGuViewModel` 内部维护本地 `MutableStateFlow<ZhiGuUiState>`，同时与 `DjiRuntime.state` 进行 `combine` 合并，形成统一 UI 状态：

- 本地演示参数、任务状态、检测状态由 ViewModel 直接维护
- DJI 初始化、注册、连接、电量、信号、相机列表由 `DjiRuntime` 提供
- 最终统一输出给界面层，避免页面分别读取多个状态源

## 6. 页面与功能模块设计

### 6.1 登录模块

功能定位：提供比赛演示入口和基础登录形式。

实现位置：

- `LoginScreen`
- `ZhiGuViewModel.login()`
- `ZhiGuViewModel.loginWithDemoAccount()`

功能内容：

- 账号输入
- 密码输入
- 普通登录按钮
- 一键“演示入口”按钮

实现特点：

- 不接真实账户系统
- 不访问服务器
- 登录成功后仅切换本地 `isLoggedIn` 状态
- 演示入口默认注入 `judge@zhigu.local`

### 6.2 首页模块

首页对应底部导航 `首页`，用于展示“总览 + 图传 + 状态摘要”。

主要组件：

- `DashboardHero`
  - 展示当前任务状态、阶段、演示评分、进度条
- `MetricRow`
  - 展示任务进度、评分、风险数、当前路径模板
- `DjiVideoPanel`
  - 图传展示核心区域
- `SceneStatusCard`
  - 展示地点、天气、同步状态、报告状态、素材数
- `LiveFeedsCard`
  - 展示多视角画面状态
- `QuickStatusCard`
  - 展示阶段、耗时、距离、图传模式、设备摘要
- `DemoTimelineCard`
  - 展示演示事件时间线
- `DemoRecordSummaryCard`
  - 展示最近演示记录摘要

业务作用：

- 让评委打开 App 后快速看到系统“像一个真实业务软件”
- 将图传、任务、检测、记录闭环汇总在一个页面讲解

### 6.3 辅助模块

辅助页对应底部导航 `辅助`，是当前版本最具展示效果的页面之一，定位为“无人机辅助感知大屏”，风格参考智能驾驶可视化感知界面。

主要能力：

- 独立辅助感知页面
- 俯视 / 侧视视角切换
- 白天 / 夜间主题切换
- 正常 / 风险逼近 / 安全通过 三种演示模式切换
- 自动轮播演示模式
- 风险色带展示
- 目标距离展示
- 目标列表与语义状态展示
- 大尺寸动态态势图展示

#### 6.3.1 视角切换

支持两种视角：

- `俯视`
  - 重点展示货车、无人机、人员、障碍物、托盘、支撑柱等对象的平面位置关系
- `侧视`
  - 重点展示车身轮廓、车头方向、地面线、目标逼近关系和预警扇区

#### 6.3.2 演示模式切换

支持三种演示模式：

- `正常`
  - 使用当前默认演示逻辑
- `风险逼近`
  - 增强风险状态、缩短目标距离、提高预警强度
- `安全通过`
  - 将目标切换到安全结果状态，适合讲解“闭环已完成”

#### 6.3.3 自动轮播

辅助页内置自动轮播逻辑，点击“自动演示”后会按以下顺序自动切换：

1. 正常
2. 风险逼近
3. 安全通过

设计目的：

- 减少演示人员手动频繁切换
- 形成更连贯的比赛展示节奏

#### 6.3.4 主题切换

支持：

- `白天`
- `夜间`

主题切换会影响：

- 背景渐变
- 文本颜色
- 信息面板风格
- 感知图整体氛围

#### 6.3.5 态势图实现

态势图通过 `Canvas` 绘制完成，包含以下视觉元素：

- 货车主体
- 无人机主体
- 人员目标框
- 障碍物目标框
- 尾部余量目标框
- 支撑柱 / 托盘 / 辅助设备示例
- 扫描线
- 波纹脉冲
- 路径引导虚线
- 风险预警扇区
- 风险色带

实现特点：

- 不依赖真实算法输入
- 所有目标态势由本地演示状态驱动
- 通过动画和目标数据切换模拟“实时感知”效果

#### 6.3.6 目标信息区

辅助页中，“目标态势”信息区已经从画布悬浮层移出，放置在态势图下方，避免遮挡画面主体。

信息区当前展示：

- 风险等级色带
- 货车状态
- 左侧目标状态与距离
- 右侧目标状态与距离
- 后方余量状态与距离
- 辅助设备识别状态

#### 6.3.7 目标列表示例

辅助页目标列表当前包含：

- 左侧人员
- 右侧障碍物
- 尾部余量
- 辅助设备
- 无人机轨迹
- 货车轮廓

每个目标包含：

- 目标名称
- 状态说明
- 距离或语义描述
- 风险等级
- 图标化标识

### 6.4 作业模块

作业页对应底部导航 `作业`，用于演示任务规划与执行过程。

主要能力：

- 车辆参数录入
  - 车牌
  - 车型
  - 货物类型
  - 货物重量
  - 重心位置
  - 紧固材料
- 路径模板选择
  - `基础交叉路径`
  - `多点均匀路径`
  - `纵横组合路径`
- 起飞前检查
  - 电量与桨叶检查
  - 牵引绳与挂点检查
  - 路径模板与禁飞区检查
  - 图传与备用视频检查
- 路径执行节点展示
  - 起点
  - 中部重点区域
  - 尾部固定点
  - 终点确认
- 路线预览与讲解建议
- 任务阶段推进
  - 准备
  - 起飞
  - 执行
  - 完成

核心方法：

- `updateVehiclePlate`
- `updateVehicleType`
- `updateCargoType`
- `updateCargoWeight`
- `updateCenterPosition`
- `updateBindingMaterial`
- `selectTemplate`
- `generateRoutePreview`
- `togglePreflightCheck`
- `advanceMissionStage`
- `resetMissionDemo`

业务定位：

- 强调“作业前准备”和“路径规划”的专业感
- 通过阶段推进制造演示节奏

### 6.5 检测模块

检测页对应底部导航 `检测`，用于展示风险识别、整改留痕和报告生成。

主要能力：

- 检测模式切换
  - 全流程检测
  - 重点区域
  - 自定义检测
- 风险项展示
  - 风险标题
  - 风险等级
  - 整改建议
  - 是否已整改
- 证据留痕
  - 整改前截图
  - 整改后截图
  - 现场备注
- 报告生成
- 演示记录生成
- 新增素材操作
- 状态闭环摘要更新

核心方法：

- `selectInspectionMode`
- `toggleFindingResolved`
- `toggleEvidenceCaptured`
- `captureDemoMedia`
- `generateInspectionReport`
- `createDemoRecord`

当前实现特点：

- 风险列表和整改状态均为本地演示数据
- 操作后会实时改写首页摘要与时间线
- 符合比赛中“发现问题 -> 整改 -> 留痕 -> 输出记录”的展示逻辑

### 6.6 设置模块

设置页对应底部导航 `设置`，是当前项目最重要的演示控制面板之一。

主要分区：

- 图传源设置
- 无人机设备状态
- 权限状态
- 当前参数总览

#### 6.6.1 图传源设置

支持三种模式：

- `自动`
  - DJI 已连接时优先实时图传
  - 未连接时若已选本地视频则播放演示视频
- `演示视频`
  - 强制播放本地选择的视频
- `DJI 实时`
  - 强制走 DJI 图传逻辑

对应字段：

- `streamMode`
- `demoVideoUri`
- `demoVideoName`
- `shouldUseDemoVideo`
- `shouldUseDjiLive`

核心方法：

- `selectStreamMode`
- `setDemoVideo`
- `clearDemoVideo`

这部分是比赛演示稳定性的关键设计：

- 真机接入成功时，可演示真实图传
- 真机接入异常时，可立即切到本地演示视频，保证展示不失控

#### 6.6.2 无人机设备状态

展示内容：

- 当前运行模式：`DEMO` / `DJI`
- 连接状态
- 注册状态
- 设备型号
- 电量
- 信号
- 网络状态
- 可用相机列表
- 最近错误信息

这部分不只用于联调，也用于比赛答辩时向评委展示“真实硬件接入能力”。

#### 6.6.3 权限状态

设置页内置权限检测与请求能力，主要用于演示设备已具备以下访问权限：

- 相机
- 定位
- 相册/视频读取

实现方式：

- 页面加载时检查权限状态
- 点击“授权”按钮后统一请求
- 结果以状态标签方式展示

#### 6.6.4 参数总览

汇总展示当前作业参数，便于讲解时快速说明现场配置。

展示字段：

- 车牌
- 车型
- 货物
- 吨位
- 重心
- 材料
- 模板
- 地点
- 天气

## 7. 图传与视频演示机制

### 7.1 设计目标

由于比赛现场存在网络、DJI 注册、遥控器连接、图传链路不稳定等风险，本项目采用“双轨图传展示机制”：

- 真实链路：DJI 图传
- 备用链路：本地演示视频

### 7.2 Demo 视频播放实现

`DemoVideoPlayer` 基于 Media3 ExoPlayer 实现：

- 从系统文件选择器选择视频
- 使用 `Uri` 加载本地媒体
- 自动 `prepare`
- 循环播放
- 默认隐藏控制条

适用场景：

- 模拟器
- 无人机未到场
- 真机图传不稳定
- 比赛答辩的保底方案

### 7.3 图传优先级逻辑

页面通过以下逻辑决定展示内容：

- 若模式为 `演示视频` 且存在已选择视频，则播放本地视频
- 若模式为 `DJI 实时` 且当前启用 DJI，则展示 DJI 图传容器
- 若模式为 `自动`
  - 已连接 DJI：优先 DJI 实时图传
  - 未连接 DJI 且存在演示视频：播放本地视频
  - 否则显示默认占位图传区

## 8. DJI 接入设计

### 8.1 接入目标

当前 DJI 接入的目标是“保留真实技术亮点，并能在条件允许时进行联调展示”，不是完整无人机控制产品。

### 8.2 初始化流程

`dji` 变体启动时，由专用 Application 调用 DJI 初始化逻辑。`DjiSdkController.initialize()` 主要流程如下：

1. 标记 DJI 模式已启用
2. 调用 `SDKManager.getInstance().init()`
3. 监听初始化事件与进度
4. 初始化完成后执行 `registerApp()`
5. 监听注册成功或失败
6. 监听产品连接、断开、变更事件
7. 注册电量、信号、产品型号、相机列表监听
8. 监听网络可用性，必要时触发重新注册

### 8.3 已接入的 DJI 状态

当前项目已经接入或预留以下 DJI 能力：

- SDK 初始化进度
- App 注册状态
- 产品连接状态
- 产品型号监听
- 电池电量监听
- 图传信号质量监听
- 可用相机列表监听
- 实时图传容器接入

当前版本的重要策略调整：

- `dji` 变体不再在应用启动阶段自动初始化 DJI SDK
- 改为在设置页手动触发初始化
- 初始化流程加入异常兜底，避免普通 Android 设备在启动阶段闪退

### 8.4 当前边界

当前未实现完整飞控业务，包括但不限于：

- 无人机自动起飞控制
- 航线规划上传
- 飞控指令下发
- 任务调度持久化
- 图像识别算法推理
- 云端回传与远程控制

因此当前 DJI 集成更适合作为比赛技术亮点展示，而不是生产飞控系统。

## 9. 权限与系统资源说明

根据 [AndroidManifest.xml](D:/Program/ANDROIDSTUDIO/WORKSPACE/ZhiGu_app/app/src/main/AndroidManifest.xml)，项目声明了以下核心权限：

- 网络相关
  - `INTERNET`
  - `ACCESS_NETWORK_STATE`
  - `ACCESS_WIFI_STATE`
  - `CHANGE_WIFI_STATE`
- 采集相关
  - `CAMERA`
  - `RECORD_AUDIO`
- 定位相关
  - `ACCESS_FINE_LOCATION`
  - `ACCESS_COARSE_LOCATION`
- 蓝牙相关
  - `BLUETOOTH`
  - `BLUETOOTH_ADMIN`
  - `BLUETOOTH_CONNECT`
  - `BLUETOOTH_SCAN`
- 媒体读取相关
  - `READ_MEDIA_IMAGES`
  - `READ_MEDIA_VIDEO`
  - `READ_EXTERNAL_STORAGE`
- USB 特性
  - `android.hardware.usb.host`
  - `android.hardware.usb.accessory`

权限用途说明：

- 相机：现场拍摄与证据留痕
- 相册/视频读取：选择本地演示视频
- 定位：模拟无人机作业环境与 DJI 依赖
- 蓝牙、网络、USB：为 DJI 连接链路预留

## 10. 数据与状态管理

### 10.1 数据来源

当前数据来源分为两类：

- 本地演示数据
  - 任务进度
  - 风险项
  - 作业参数
  - 演示记录
  - 时间线事件
- 设备实时数据
  - DJI 注册状态
  - 连接状态
  - 电量
  - 信号
  - 相机列表

### 10.2 持久化现状

当前版本没有接入以下能力：

- Room 数据库
- SharedPreferences 持久化配置
- 服务端接口
- 账号鉴权

因此：

- 重新启动应用后，大部分演示数据会恢复为默认值
- 本地选择的视频 URI 是否可继续访问，取决于系统授权与文件来源

## 11. 已完成度评估

从“比赛演示版”角度看，当前项目完成度较高，主要体现在：

- App 可正常编译、安装、运行
- 登录页、首页、辅助页、作业页、检测页、设置页均已成型
- 演示流程具备较强可讲解性
- 图传区已支持占位、演示视频、DJI 实时三种策略
- 设置页可以承担比赛现场的演示控制功能
- DJI 技术亮点已经保留并形成独立变体
- 辅助页已经形成较强的比赛级动态展示能力

从“生产应用”角度看，当前仍属于原型/展示系统，尚缺少：

- 后端服务
- 数据持久化
- 真实业务闭环
- 算法模型接入
- 异常恢复机制
- 真机飞控级安全设计

## 12. 后续开发建议

### 12.1 如果继续做比赛版本增强

优先建议：

- 增强演示脚本与一键切换场景
- 补充更多样本视频与截图素材
- 为不同车辆/货物类型预置更多模板
- 增加报告导出展示页
- 增加更真实的设备异常提示与状态动画
- 若继续强化“辅助感知屏”，可进一步接入真实图标素材或演示语音播报

### 12.2 如果未来转向实际应用

需要补齐：

- 后端接口与账户体系
- 本地数据库
- 作业数据持久化
- 真实图像识别能力
- DJI 飞行控制与任务管理
- 多端协同与日志体系

## 13. 结论

智固 App 当前已经形成一个适合比赛演示的 Android 原型系统。其核心价值不在于完整业务生产化，而在于：

- 界面完整
- 演示逻辑连贯
- 支持本地保底方案
- 保留 DJI 接入技术亮点

这一版本适合继续围绕“讲解效果、演示稳定性、技术展示感”三个方向持续优化。
